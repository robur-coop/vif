(cram
 (package vif)
 (deps
  %{bin:vif}
  waitfile.exe
  waitport.exe
  (package vif)
  (source_tree examples)))

(executable
 (name waitfile)
 (libraries unix))

(executable
 (name waitport)
 (libraries unix))
